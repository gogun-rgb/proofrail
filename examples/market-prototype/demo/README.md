# Market prototype demo

This directory is a deterministic local consumer fixture for the public prototype. The checked-in configuration selects the `docs-only` preset and runs one bounded `git diff --check` command.
